import { render, screen } from '@testing-library/react';
import Navbar from './index';

describe("navbar test", () => {
    

    test("aLink", () => {
        // render(<Navbar  />);

        // const aTag = screen.getByText("Starships");
        // expect(aTag).toBeInTheDocument();
    })
})